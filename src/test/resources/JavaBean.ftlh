package ${package};

import lombok.*;

/**
 * <P>
    * ${model.description}
    * </P>
 * Created by ${author} on ${date}
 **/
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class ${modelName} {

    private ${property.type} ${proertyName};



}
